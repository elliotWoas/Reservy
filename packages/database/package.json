{
  "name": "@reservy/database",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:seed": "bun run ./prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "@reservy/domain": "workspace:*"
  },
  "devDependencies": {
    "prisma": "^5.14.0"
  }
}
